<div class="m-[17px]">
    <div
      class="p-4 bg-white block sm:flex items-center justify-between border-b border-gray-200 lg:mt-1.5 dark:bg-gray-800 dark:border-gray-700"
    >
      <div class="w-full mb-1">
        <div class="mb-4">
          <h1
            class="text-xl font-semibold text-gray-900 sm:text-2xl dark:text-white"
          >
            <%= oldData?.id ? 'Edit Role' : 'Add Role' %>
          </h1>
        </div>
      </div>
    </div>
    <div class="flex flex-col">
      <div class="overflow-x-auto">
        <div class="inline-block min-w-full align-middle">
          <div class="overflow-hidden shadow">
            <div class="p-6 space-y-6 5 dark:bg-gray-800">
              <% const fieldError = (fieldName) => { const err = errors?.find(e =>
              e.path === fieldName); return err ? err.msg : null; }; %>
              <form
                action="/save-role"
                method="POST"
                enctype="multipart/form-data"
              >
                <% if (oldData?.id) { %>
                <input type="hidden" name="_method" value="PUT" />
                <input type="hidden" name="id" value="<%= oldData.id %>" />
                <% } %>
  
                <div class="grid grid-cols-6 gap-6">
                  <div class="col-span-6 sm:col-span-3 ">
                    <label
                      for="name"
                      class="block mb-2 text-sm font-medium text-gray-900 dark:text-white"
                    >
                      Name
                      <span class="text-red-500">*</span>
                    </label>
                    <input
                      type="text"
                      name="name"
                      id="name"
                      class="shadow-sm bg-gray-50 border border-gray-300 text-gray-900 sm:text-sm rounded-lg focus:ring-primary-500 focus:border-primary-500 block w-full p-2.5 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:ring-primary-500 dark:focus:border-primary-500"
                      placeholder="Role Name"
                      required
                      value="<%= oldData?.name || '' %>"
                    />
                    <% if (fieldError('name')) { %>
                    <p class="text-red-500 text-xs mt-1">
                      <%= fieldError('name') %>
                    </p>
                    <% } %>
                  </div>
                  <div class="col-span-6 sm:col-span-3">
                    <label
                      for="status"
                      class="block mb-2 text-sm font-medium text-gray-900 dark:text-white"
                    >
                      Permission
                      <span class="text-red-500">*</span>
                    </label>
                    <select
                    id="permissions"
                    name="permissions"
                    multiple
                    class="select2 shadow-sm bg-gray-50 border border-gray-300 text-gray-900 sm:text-sm rounded-lg focus:ring-primary-500 focus:border-primary-500 block w-full p-2.5 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white"
                    required
                  >
                    <% permissions.forEach(permission => { %>
                      <option value="<%= permission._id %>" 
                        <%= oldData?.permissions?.includes(permission._id.toString()) ? 'selected' : '' %>>
                        <%= permission.name %>
                      </option>
                      
                    <% }) %>
                
                  </select>
                  </div>
                </div>
     
                <div 
                class="items-center pt-6 mt-6 border-t border-gray-200 rounded-b dark:border-gray-700 flex justify-end gap-2">
                  <a href="/roles"
                      class="text-white bg-red-600 hover:bg-red-700 focus:ring-4 focus:ring-red-300 font-medium rounded-lg text-sm px-5 py-2.5 text-center dark:bg-red-500 dark:hover:bg-red-300 dark:focus:ring-red-800">
                    Go Back
                  </a>
                  <button
                    class="text-white bg-primary-700 hover:bg-primary-800 focus:ring-4 focus:ring-primary-300 font-medium rounded-lg text-sm px-5 py-2.5 text-center dark:bg-primary-600 dark:hover:bg-primary-700 dark:focus:ring-primary-800"
                    type="submit">
                    <%= oldData?.id ? 'Update' : 'Submit' %>
                  </button>
                </div>
              </form>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
  <style>
  /* 🌗 Shared base styling for select2 multiselect */
  .select2-container--default .select2-selection--multiple {
    background-color: #f9fafb; /* light mode: Tailwind gray-50 */
    border: 1px solid #d1d5db; /* gray-300 */
    border-radius: 0.5rem; /* rounded-lg */
    padding: 0.375rem 0.5rem;
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    min-height: 42px;
    gap: 0.25rem;
  }

  .dark .select2-container--default .select2-selection--multiple {
    background-color: #1f2937; /* dark: gray-800 */
    border-color: #4b5563;     /* dark: gray-600 */
    color: #fff;
  }

  /* ✅ Fix extra space + blinking cursor */
  /* .select2-container--default .select2-selection--multiple .select2-search__field {
    height: 0 !important;
    margin: 0 !important;
    padding: 0 !important;
    line-height: 0 !important;
    border: none !important;
    background: transparent !important;
    color: transparent !important;
    caret-color: transparent !important;
    width: 0 !important;
    flex: 0 0 0 !important;
  } */

  /* ✅ Selected tag styling (roles) */
  .select2-container--default .select2-selection__choice {
    background-color: #2563eb; /* Tailwind blue-600 */
    color: #fff;
    font-size: 0.75rem; /* text-sm */
    padding: 2px 8px;
    border: none;
    border-radius: 0.375rem; /* rounded */
    margin-top: 4px;
  }

  .dark .select2-container--default .select2-selection__choice {
    background-color: #3b82f6; /* blue-500 */
    color: white;
  }

  /* ❌ Remove "x" hover color change on dark */
  .select2-container--default .select2-selection__choice__remove {
    color: #fff;
    margin-right: 6px;
    font-weight: bold;
  }

  .dark .select2-container--default .select2-selection__choice__remove:hover {
    background-color: transparent;
    color: #e0e7ff; /* blue-100 */
  }

  /* ✅ Dropdown styling */
  .select2-dropdown {
    border-radius: 0.5rem;
    border-color: #d1d5db;
    font-size: 0.875rem;
  }

  .select2-container--default .select2-results__option {
    padding: 0.5rem 0.75rem;
  }

  .select2-container--default .select2-results__option--highlighted[aria-selected] {
    background-color: #3b82f6; /* blue-500 */
    color: white;
  }

  .dark .select2-dropdown {
    background-color: #1f2937;
    color: white;
    border-color: #4b5563;
  }

  .dark .select2-results__option {
    color: white;
  }

  .select2-container--default .select2-selection--multiple .select2-selection__choice__display{
    color: black !important;
    font-size: 0.95rem;
  }

  .dark .select2-container--default .select2-selection--multiple .select2-selection__choice__display{
    color: #fff !important;
    font-size: 0.95rem;
  }

  .dark .select2-container--default .select2-selection--multiple{
    padding-top: 0px !important;
  }

  .select2-container--default .select2-selection--multiple{
    padding-top: 0px !important;
  }

  .select2-container--default .select2-selection--multiple .select2-selection__choice__remove span{
    font-size: 0.95rem;
  }

  #roles option:disabled {
    color: gray;
    font-style: italic;
  }
  .select2-results__option[aria-disabled="true"] {
    color: gray !important;
    cursor: not-allowed !important;
  }

  .dark .select2-results__option[aria-disabled="true"] {
    color: #9ca3af !important;
    background-color: #1f2937 !important;
    cursor: not-allowed !important;
  }


</style>