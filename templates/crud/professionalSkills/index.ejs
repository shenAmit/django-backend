<div class="m-[17px]">
  <div
    class="p-4 bg-white block sm:flex items-center justify-between border-b border-gray-200 lg:mt-1.5 dark:bg-gray-800 dark:border-gray-700"
  >
    <div class="w-full mb-1">
      <div class="mb-4">
        <h1
          class="text-xl font-semibold text-gray-900 sm:text-2xl dark:text-white"
        >
          All Professional Skills
        </h1>
      </div>
      <div class="sm:flex">
        <div
          class="items-center hidden mb-3 sm:flex sm:divide-x sm:divide-gray-100 sm:mb-0 dark:divide-gray-700"
        >
          <form class="lg:pr-3" method="GET">
            <label for="users-search" class="sr-only">Search</label>
            <div class="relative mt-1 lg:w-64 xl:w-96 flex gap-4">
              <input
                type="text"
                name="name"
                id="name"
                class="bg-gray-50 border border-gray-300 text-gray-900 sm:text-sm rounded-lg focus:ring-primary-500 focus:border-primary-500 block w-full p-2.5 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:ring-primary-500 dark:focus:border-primary-500"
                placeholder="Search Professional Skill"
                value="<%= search || '' %>"
              />
              <button
                class="text-white bg-primary-700 hover:bg-primary-800 focus:ring-4 focus:ring-primary-300 font-medium rounded-lg text-sm px-5 py-2.5 text-center dark:bg-primary-600 dark:hover:bg-primary-700 dark:focus:ring-primary-800"
                type="submit"
              >
                Submit
              </button>
            </div>
          </form>
        </div>
        <% if (user?.permissions?.includes("professional_skill_create")) { %>
        <div class="flex items-center ml-auto space-x-2 sm:space-x-3">
          <button
            id="addProfessionalSkillBtn"
            type="button"
            class="inline-flex items-center justify-center w-1/2 px-3 py-2 text-sm font-medium text-center text-white rounded-lg bg-primary-700 hover:bg-primary-800 focus:ring-4 focus:ring-primary-300 sm:w-auto dark:bg-primary-600 dark:hover:bg-primary-700 dark:focus:ring-primary-800"
          >
            <svg
              class="w-5 h-5 mr-2 -ml-1"
              fill="currentColor"
              viewBox="0 0 20 20"
              xmlns="http://www.w3.org/2000/svg"
            >
              <path
                fill-rule="evenodd"
                d="M10 5a1 1 0 011 1v3h3a1 1 0 110 2h-3v3a1 1 0 11-2 0v-3H6a1 1 0 110-2h3V6a1 1 0 011-1z"
                clip-rule="evenodd"
              ></path>
            </svg>
            Add Professional Skill
          </button>
        </div>
        <% } %>
      </div>
    </div>
  </div>

  <div class="flex flex-col">
    <div class="overflow-x-auto">
      <div class="inline-block min-w-full align-middle">
        <div class="overflow-hidden shadow">
          <table
            class="min-w-full divide-y divide-gray-200 table-fixed dark:divide-gray-600"
          >
            <thead class="bg-gray-100 dark:bg-gray-700">
              <tr>
                <th
                  scope="col"
                  class="p-4 whitespace-nowrap text-xs font-medium text-left text-gray-500 uppercase dark:text-gray-400"
                >
                  S No.
                </th>
                <th
                  scope="col"
                  class="p-4 text-xs font-medium text-left text-gray-500 uppercase dark:text-gray-400"
                >
                  Name
                </th>
                <th
                  scope="col"
                  class="p-4 text-xs font-medium text-left text-gray-500 uppercase dark:text-gray-400"
                >
                  Status
                </th>
                <th
                  scope="col"
                  class="p-4 text-xs font-medium text-left text-gray-500 uppercase dark:text-gray-400"
                >
                  Added At
                </th>
                <% if (user?.permissions?.includes("professional_skill_edit") ||
                user?.permissions?.includes("professional_skill_delete")) { %>

                <th
                  scope="col"
                  class="p-4 text-xs font-medium text-left text-gray-500 uppercase dark:text-gray-400"
                >
                  Actions
                </th>
                <% } %>
              </tr>
            </thead>
            <tbody
              class="bg-white divide-y divide-gray-200 dark:bg-gray-800 dark:divide-gray-700"
            >
              <% if (professionalSkills.length === 0) { %>
              <tr>
                <td
                  colspan="5"
                  class="p-4 text-center text-gray-500 dark:text-gray-400"
                >
                  No Professional Skill found.
                </td>
              </tr>
              <% } else { %> <% for (let i = 0; i < professionalSkills.length; i++) { %> <% const professionalSkill = professionalSkills[i]; %>
              <tr class="hover:bg-gray-100 dark:hover:bg-gray-700">
                <td class="p-4 text-sm text-gray-900 dark:text-white">
                  <%= ((pagination.perPage * (pagination.currentPage - 1)) + i + 1) %>
                </td>
                <td class="p-4 text-sm text-gray-900 dark:text-white">
                  <%= professionalSkill.name %>
                </td>
                <td class="p-4 text-sm text-gray-900 dark:text-white">
                  <% if (professionalSkill.status) { %>
                    <span class="inline-flex items-center rounded-md bg-green-50 px-2 py-1 text-xs font-medium text-green-700 ring-1 ring-inset ring-green-600/20">
                      Active
                    </span>
                  <% } else { %>
                    <span class="inline-flex items-center rounded-md bg-red-50 px-2 py-1 text-xs font-medium text-red-700 ring-1 ring-inset ring-red-600/20">
                      Inactive
                    </span>
                  <% } %>
                </td>
                <td class="p-4 text-sm text-gray-900 dark:text-white">
                  <%= professionalSkill.createdAt.toLocaleDateString('en-US', { year: 'numeric', month: '2-digit', day: '2-digit', }) %>
                </td>

                <td class="p-4">
                  <div class="flex items-center space-x-2">
                  <% if (user?.permissions?.includes("professional_skill_edit")) { %>
                  <button
                    type="button"
                    class="edit-professional-skill-btn text-primary-600 hover:text-primary-900 dark:text-primary-400 dark:hover:text-primary-300"
                    data-id="<%= professionalSkill._id %>"
                    data-name="<%= professionalSkill.name %>"
                    data-status="<%= professionalSkill.status %>"
                    title="Edit Professional Skill"
                  >
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"
                      xmlns="http://www.w3.org/2000/svg">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                        d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z">
                      </path>
                    </svg>
                  </button>
                  <% } %> <% if (user?.permissions?.includes("professional_skill_delete")) { %>
                  <button
                    type="button"
                    class="delete-professional-skill-btn text-red-600 hover:text-red-900 dark:text-red-400 dark:hover:text-red-300"
                    data-id="<%= professionalSkill._id %>"
                    data-name="<%= professionalSkill.name %>"
                    title="Delete Professional Skill"
                  >
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"
                      xmlns="http://www.w3.org/2000/svg">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                        d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16">
                      </path>
                    </svg>
                  </button>
                  <% } %>
                  </div>
                </td>
              </tr>
              <% } %> <% } %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>

  <%- include('../components/pagination', {
    pagination,
    search,
    query,
    queryString
  }) %>
</div>

<!-- Professional Skill Add/Edit Modal -->
<div id="professionalSkillModal" class="fixed inset-0 z-50 hidden overflow-y-auto">
  <div class="flex items-center justify-center min-h-screen px-4 pt-4 pb-20 text-center sm:block sm:p-0">
    <!-- Background overlay with blur effect -->
    <div class="fixed inset-0 transition-all duration-300 bg-gray-500 bg-opacity-75 backdrop-blur-sm modal-backdrop"></div>

    <!-- Modal panel -->
    <div class="inline-block w-full max-w-md p-6 my-8 overflow-hidden text-left align-middle transition-all transform bg-white shadow-xl rounded-lg dark:bg-gray-800 modal-content">
      <div class="flex items-center justify-between mb-4">
        <h3 id="professionalSkillModalTitle" class="text-lg font-medium text-gray-900 dark:text-white">
          Add Professional Skill
        </h3>
        <button type="button" class="modal-close text-gray-400 hover:text-gray-600 dark:hover:text-gray-300">
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
          </svg>
        </button>
      </div>

      <form id="professionalSkillForm">
        <input type="hidden" id="professionalSkillId" name="id" value="">

        <div class="mb-4">
          <label for="professionalSkillName" class="block mb-2 text-sm font-medium text-gray-900 dark:text-white">
            Professional Skill Name <span class="text-red-500">*</span>
          </label>
          <input
            type="text"
            id="professionalSkillName"
            name="name"
            class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-primary-500 focus:border-primary-500 dark:bg-gray-700 dark:border-gray-600 dark:text-white"
            placeholder="Enter professional skill name"
            required
          />
        </div>

        <div class="mb-6">
          <label for="professionalSkillStatus" class="block mb-2 text-sm font-medium text-gray-900 dark:text-white">
            Status
          </label>
          <select
            id="professionalSkillStatus"
            name="status"
            class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-primary-500 focus:border-primary-500 dark:bg-gray-700 dark:border-gray-600 dark:text-white"
          >
            <option value="true">Active</option>
            <option value="false">Inactive</option>
          </select>
        </div>

        <div class="flex justify-end space-x-3">
          <button
            type="button"
            class="px-4 py-2 text-sm font-medium text-gray-700 bg-gray-200 rounded-lg hover:bg-gray-300 dark:bg-gray-600 dark:text-gray-200 dark:hover:bg-gray-500 modal-close"
          >
            Cancel
          </button>
          <button
            type="submit"
            id="professionalSkillSubmitBtn"
            class="px-4 py-2 text-sm font-medium text-white bg-primary-600 rounded-lg hover:bg-primary-700 focus:ring-4 focus:ring-primary-300 dark:bg-primary-600 dark:hover:bg-primary-700 dark:focus:ring-primary-800"
          >
            Submit
          </button>
        </div>
      </form>
    </div>
  </div>
</div>

<!-- Delete Confirmation Modal -->
<div id="deleteProfessionalSkillModal" class="fixed inset-0 z-50 hidden overflow-y-auto">
  <div class="flex items-center justify-center min-h-screen px-4 pt-4 pb-20 text-center sm:block sm:p-0">
    <!-- Background overlay with blur effect -->
    <div class="fixed inset-0 transition-all duration-300 bg-gray-500 bg-opacity-75 backdrop-blur-sm modal-backdrop"></div>

    <!-- Modal panel -->
    <div class="inline-block w-full max-w-md p-6 my-8 overflow-hidden text-left align-middle transition-all transform bg-white shadow-xl rounded-lg dark:bg-gray-800 modal-content">
      <div class="flex items-center justify-between mb-4">
        <h3 class="text-lg font-medium text-gray-900 dark:text-white">
          Delete Professional Skill
        </h3>
        <button type="button" class="modal-close text-gray-400 hover:text-gray-600 dark:hover:text-gray-300">
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
          </svg>
        </button>
      </div>

      <div class="mb-6">
        <p class="text-sm text-gray-600 dark:text-gray-300">
          Are you sure you want to delete the professional skill "<span id="deleteProfessionalSkillName" class="font-medium"></span>"? This action cannot be undone.
        </p>
      </div>

      <div class="flex justify-end space-x-3">
        <button
          type="button"
          class="px-4 py-2 text-sm font-medium text-gray-700 bg-gray-200 rounded-lg hover:bg-gray-300 dark:bg-gray-600 dark:text-gray-200 dark:hover:bg-gray-500 modal-close"
        >
          Cancel
        </button>
        <button
          type="button"
          id="confirmDeleteProfessionalSkill"
          class="px-4 py-2 text-sm font-medium text-white bg-red-600 rounded-lg hover:bg-red-700 focus:ring-4 focus:ring-red-300 dark:bg-red-600 dark:hover:bg-red-700 dark:focus:ring-red-800 disabled:opacity-50 disabled:cursor-not-allowed"
        >
          <span class="delete-btn-text">Delete</span>
          <span class="delete-btn-loader hidden inline-flex items-center">
            <svg class="animate-spin -ml-1 mr-2 h-4 w-4 text-white" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
              <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
              <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
            </svg>
            Deleting...
          </span>
        </button>
      </div>
    </div>
  </div>
</div>

<style>
  /* Modal animations and blur effects */
  #professionalSkillModal, #deleteProfessionalSkillModal {
    backdrop-filter: blur(4px);
    -webkit-backdrop-filter: blur(4px);
  }

  .modal-content {
    animation: modalSlideIn 0.3s ease-out;
  }

  @keyframes modalSlideIn {
    from {
      opacity: 0;
      transform: translateY(-20px) scale(0.95);
    }
    to {
      opacity: 1;
      transform: translateY(0) scale(1);
    }
  }

  /* Enhanced backdrop blur */
  .modal-backdrop {
    backdrop-filter: blur(6px);
    -webkit-backdrop-filter: blur(6px);
  }
</style>